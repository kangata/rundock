services:
  redis:
    image: redis::tag
    restart: always
    volumes:
      - ${RUNDOCK_PATH}/data/redis:/data
    logging:
      options:
        max-file: '1'
        max-size: '1m'
    environment:
      - TZ=Asia/Jakarta
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      retries: 3
      timeout: 5s